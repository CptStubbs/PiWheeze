# Bcrypt hash of the dashboard basic-auth password (username: piwheeze).
# Generate with:
#   docker run --rm caddy:2-alpine caddy hash-password
# Then copy this file to .env and replace the value below.
DASHBOARD_BASIC_AUTH_HASH=$2a$14$REPLACE_ME
